{
  "name": "hotelsearchproject",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:run": "npx cypress run --reporter junit > cypress.log 2>&1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.17.0",
    "cypress-junit-reporter": "^2.1.0"
  },
  "overrides": {
    "string-width-cjs": "5.1.1",
    "strip-ansi-cjs": "5.1.0"
  }
}
